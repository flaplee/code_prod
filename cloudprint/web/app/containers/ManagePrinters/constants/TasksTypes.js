import key from '../key';

export const REQUEST_TASKS = `${key}/REQUEST_TASKS`;
export const RECEIVE_TASKS = `${key}/RECEIVE_TASKS`;
export const TASKS_ERROR = `${key}/TASKS_ERROR`;

export const SET_TASKS_PAGE = `${key}/SET_TASKS_PAGE`;
